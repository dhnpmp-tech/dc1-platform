# DC1 Gate 0 — Security Checklist

**For:** Peter (Go/No-Go verification)
**Date:** 2026-03-02
**Reference:** `docs/SECURITY-AUDIT-GATE0.md`

---

## Pre-Launch Checklist

### Authentication & Authorization
- [ ] All public API endpoints require authentication (API key or MC_TOKEN)
- [ ] `GET /api/security/events` requires `x-mc-token` header (**currently missing**)
- [ ] `GET /api/security/summary` requires `x-mc-token` header (**currently missing**)
- [ ] `GET /api/standup/latest` requires authentication
- [x] `POST /api/security/flag/:id` requires MC_TOKEN ✅
- [x] `POST /api/standup/run` requires Bearer MC_TOKEN ✅
- [x] `POST /api/providers/heartbeat` validates API key ✅

### Secrets & Credentials
- [ ] No hardcoded tokens in source code (check `docker-manager.ts` line 15, `job-pipeline.ts` line 10)
- [ ] `MC_TOKEN` is set via environment variable only (no default fallback)
- [x] Supabase service key loaded from env var ✅
- [x] No API keys logged to stdout ✅
- [ ] Provider API keys not exposed in URL paths (move to Authorization header)

### Input Validation & Injection
- [x] SQL queries use parameterized statements (`?` placeholders) ✅
- [x] No SQL string concatenation in any route ✅
- [ ] Shell injection protection on `/api/providers/setup?key=` parameter
- [ ] Job submission validates `dockerImage` against allowlist
- [ ] Registration validates email format (not just presence)

### Rate Limiting
- [ ] Rate limiting active on `/api/providers/register` (max 5/min per IP)
- [ ] Rate limiting active on `/api/providers/heartbeat` (max 15/min per key)
- [ ] Rate limiting on failed auth attempts

### Network & Transport
- [ ] CORS restricted to specific origins (not wildcard `*`)
- [ ] HTTPS enforced in production
- [ ] HSTS header configured
- [x] GPU containers run with `NetworkMode: 'none'` (no internet) ✅

### Container Security
- [x] Job code mounted read-only (`:ro`) ✅
- [x] Memory and CPU limits enforced ✅
- [x] GPU memory wiped between jobs ✅
- [x] Containers labeled for tracking (`dc1.job_id`, `dc1.managed_by`) ✅
- [ ] Docker images restricted to trusted registry

### Billing & Wallet
- [x] All math in halala (integer) — no floating-point currency ✅
- [x] Atomic debit via `SELECT FOR UPDATE` — no double-spend ✅
- [x] Cryptographic proof hash on every billing tick ✅
- [x] Billing verification endpoint available ✅
- [x] Idempotent session/tick/receipt creation ✅
- [x] 75/25 provider/DC1 split enforced with remainder-safe rounding ✅

### Monitoring & Audit
- [x] Security events dashboard tracks stale heartbeats ✅
- [x] Suspicious toggle detection (>3 in 1 hour) ✅
- [x] Provider status change logging (`provider_status_log`) ✅
- [ ] Audit logging for Express routes (providers, standup)

---

## Verdict Summary

| Category | Status |
|----------|--------|
| SQL Injection | ✅ PASS |
| Billing Integrity | ✅ PASS |
| Container Isolation | ✅ PASS |
| Authentication | ⚠️ PARTIAL — 3 endpoints need auth |
| Rate Limiting | ❌ MISSING |
| CORS/Transport | ⚠️ NEEDS CONFIG |

**Overall: CONDITIONAL PASS** — Fix 3 items marked above before Go/No-Go.

---

*Generated by GUARDIAN — DC1 Security Engineer*
